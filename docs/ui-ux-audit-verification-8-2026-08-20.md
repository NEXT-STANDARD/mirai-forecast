# 再修正 検証レポート（第8回）

**対象**: mirai-forecast / 第7回検証の指摘に対する再修正
**検証日**: 2026-08-20
**検証方法**: ソース照合 ＋ 本番ビルド（`vite build` → `vite preview`）実機計測 ＋ Supabase 実データ照会

---

## 総括

**NEW-4 は完全に解消しました。指摘した2点とも、報告どおりです。**

これで**監査後に検出した新規問題5件のうち4件が解決**し、残るのは実害のない `NEW-2`（dev と本番の CSS 出力差異）のみになりました。
今回も**回帰はゼロ**で、3ラウンド連続です。

| 判定 | 第1回 | … | 第6回 | 第7回 | 今回 |
|---|---:|---:|---:|---:|---:|
| ✅ 完全に修正（35項目中） | 9 | … | 26 | 26 | **26** |
| ⚠️ 部分的・不完全 | 5 | … | 2 | 2 | **2** |
| ❌ 未達 | 21 | … | 7 | 7 | **7** |

| 新規問題 | 状態 |
|---|---|
| `NEW-1` PWAバナーがフッターリンクを覆う | ✅ 解決 |
| `NEW-2` dev と本番で CSS 出力が異なる | ❌ 未対応（本番影響なし） |
| `NEW-3` 固定バーと余白の不整合 | ✅ 解決 |
| `NEW-4` 締切切れ銘柄が投票可能 | ✅ **今回解決** |
| `NEW-5` 締切表示から年が落ちる | ✅ 解決 |

---

## ✅ NEW-4(1) — 投票導線のガードが全面配備された

第7回で提示した「`onVote` を持つコンポーネントを列挙して突き合わせる」手順で再確認しました。

```
AllMarketsGrid.tsx         onVote:2 isExpired:1 使用元:1  ✅
EventModal.tsx             onVote:2 isExpired:1 使用元:1  ✅ ← 今回追加
MobileStickyVoteBar.tsx    onVote:1 isExpired:3 使用元:1  ✅ ← 今回追加
SpreadRankingSection.tsx   onVote:2 isExpired:1 使用元:1  ✅ ← 今回追加
OrderBookConsensus.tsx     onVote:1 isExpired:1 使用元:2  ✅
MarketDetailPage.tsx       onVote:1 isExpired:0 使用元:1  ✅（OrderBookConsensus 経由で保護）
EventCard.tsx              onVote:2 isExpired:1 使用元:0  ─ デッドコード
HeroFeatured.tsx           onVote:1 isExpired:1 使用元:0  ─ デッドコード
```

**ガード漏れ 0件。** 条件式も全コンポーネントで統一されています。

```js
event.isExpired || (event.endDate && new Date(event.endDate).getTime() < Date.now())
```

`MarketDetailPage` は自前の投票ボタンを持たず、`onVote` を `OrderBookConsensus` に渡すだけなので、
`isExpired` を直接持たなくても保護されています（`MarketDetailPage.tsx:411`）。

> **検証上の留保**：締切切れ銘柄がデータから無くなったため、
> 今回追加された3つのガードが**実際に発火する様子は観測できませんでした。**
> ただし条件式は、第7回で実際に発火を確認済みの `AllMarketsGrid` / `OrderBookConsensus` と同一です。

---

## ✅ NEW-4(2) — Supabase の締切切れが 0件に

```
全レコード      : 96
is_active=true  : 68
うち締切切れ     : 0 件   ← 報告どおり
```

第7回で残っていた2件（`411239` エチオピア首相 / `699735` イラン封鎖）が非活性化されました。

### 再有効化の防止も入った

第7回で「同期のたびに再有効化されるなら、実行順序を見直すべき」と書いた点について、
`translate_all_supabase_events.mjs` にも期限判定が入りました。

```js
const isExpired = ev.end_date ? new Date(ev.end_date) < new Date() : false;
...
is_active: !isExpired,
```

`sync_polymarket_cron.mjs`（`is_active: !isExpired`）と合わせて、
**DB を書き換える2経路の両方で期限が判定される**ようになりました。これが2件の取りこぼしの原因だったと考えられます。

### 現在のデータ状態

締切が最も近い銘柄は本日 16:00 UTC（明日 01:00 JST）で、いずれも未到来です。
画面上の29カードすべてが有効期限内で、「受付終了」バッジは 0件でした。**想定どおりの状態です。**

---

## ⚠️ 軽微：デッドコード2件は削除ではなくガード追加で対応された

第7回で `EventCard.tsx` / `HeroFeatured.tsx` は参照0件のデッドコードなので**削除**を提案しましたが、
今回は両方に `isExpired` ガードが追加されました（使用元は依然0件）。

「ガード漏れ0件」を機械的に満たす意味では正しい対応です。
ただし**使われないコードの保守コストは残ります**。今後どちらかを復活させる予定がないなら、削除するほうが健全です。

---

## ❌ 未達（7件・変化なし）

`A-7`（コメント永続化）/ `B-3`（CSS分割）/ `C-4`（画像）/ `D-5`（強制スクロール）/
`D-6`（ナビ二重）/ `E-4`（非ボタン要素）/ `F-3`（ルーティング）

`D-4`（固定バーの誤爆）と `F-1`（CSS未分割）も継続です。

---

## ✅ 維持されている指標（回帰ゼロ・3ラウンド連続）

| 指標 | 第7回 | 今回 |
|---|---:|---:|
| タップ 44px 未満 | 0% | **0%**（0/144） |
| 12px 未満のテキスト | 3% | **3%**（最頻値 12.0px・10px未満 3件） |
| コントラスト AA 不合格 | 3件 | **3件** |
| 英語・破綻タイトル | 0件 | **0件** |
| 画面上の過去日付 | 0件 | **0件** |
| トップページの `h1` | 1 | **1** |
| 型チェック | exit 0 | **exit 0** |
| JS 初期チャンク | 605.5kB | **606.7kB** |

---

## 次にやるべきこと

新規問題の対応は事実上完了しました。**ここからは監査の未達7件が主戦場になります。**

### 短期（半日〜1日ずつ）

1. **`D-5`** カード押下で最上部へ強制スクロール — 遷移先を詳細ページに統一するだけ
2. **`D-6`** カテゴリナビ二重 — 定義を1箇所に統合し、未実装の `'newest'` ソートを削除
3. **`C-4`** `iconUrl` を描画 — 型にはあるが未使用。カードの走査性が上がる
4. デッドコード（`EventCard` / `HeroFeatured`）の削除

### 中期（数日〜）

5. **`E-4`** `<tr onClick>` / `<div onClick>` のキーボード対応
6. **`A-7`** コメントの永続化（または機能ごと非表示）
7. **`F-1` / `B-3`** 管理コンソール CSS の切り出しと `index.css` の分割
8. **`F-3`** ルーティングの整理（`App.tsx` の早期 return と真偽値5個）

---

## 所感

**8ラウンドを経て、新規に検出した問題はほぼ片付きました。**

とくに直近3ラウンドは回帰ゼロで、修正の質が安定しています。
今回の NEW-4 対応は、第7回で提示した「`onVote` を grep して全部に同じガードを入れる」という手順が
そのまま実行された形で、**指摘の意図を取り違えずに機械的に潰し切っています。**
再有効化の防止まで含めて、DB を書き換える経路を2つとも塞いだ点も的確でした。

残る7件は、いずれも**一度も着手されていない項目**です。
これらは「直したつもりが直っていない」類ではなく、単に手が回っていないもので、
`D-5` `D-6` `C-4` あたりは半日ずつで終わる規模です。

監査開始時点の主要指標と比べると、状況は大きく変わりました。

```
              監査時      現在
12px未満       86%    →   3%
タップ44px未満  90%    →   0%
コントラスト不合格 28%   →  0.3%（3件）
無スタイル要素   23%    →   3%
英語タイトル     6件    →   0件
h1              0      →   1
```

---

## 付録：検証手順

```bash
npx vite build
npx vite preview --host 127.0.0.1 --port 4173
npx tsc -b
```

投票導線の網羅チェック（第7回で提示したもの）:

```bash
for f in src/components/*.tsx; do
  v=$(grep -c 'onVote(' "$f"); [ "$v" = "0" ] && continue
  printf "%-26s onVote:%s isExpired:%s\n" "$(basename $f)" "$v" "$(grep -c 'isExpired' $f)"
done
```

締切切れ銘柄の検出:

```bash
curl -s "$VITE_SUPABASE_URL/rest/v1/events?select=title_ja,end_date,is_active&limit=300" \
  -H "apikey: $VITE_SUPABASE_ANON_KEY" -H "Authorization: Bearer $VITE_SUPABASE_ANON_KEY"
```
