# 再修正 検証レポート（第9回）

**対象**: mirai-forecast / 第8回検証の指摘に対する再修正
**検証日**: 2026-08-20
**検証方法**: ソース照合 ＋ 本番ビルド（`vite build` → `vite preview`）実機計測 ＋ Supabase 実データ照会

---

## 総括

| 判定 | 第1回 | … | 第7回 | 第8回 | 今回 |
|---|---:|---:|---:|---:|---:|
| ✅ 完全に修正（35項目中） | 9 | … | 26 | 26 | **28** |
| ⚠️ 部分的・不完全 | 5 | … | 2 | 2 | **3** |
| ❌ 未達 | 21 | … | 7 | 7 | **4** |

**未達が7件から4件に減りました。** 監査開始時の21件から、5分の1以下です。

- **D-5・C-4・デッドコード削除は完全に解消**
- **D-6 は「定義の重複」は解消したが「表示の重複」が残っている**（本丸が未対応）
- 検証中に**新たな問題を1件**発見しました。A-3 の是正が別コンポーネントに適用されていません

回帰は4ラウンド連続でゼロです。

---

## ✅ D-5 — カード押下の挙動が統一された

`AllMarketsGrid.tsx:194` が `onOpenDetail` を呼ぶようになり、
`App.tsx` の `onSelectEvent` もすべて `handleOpenMarketDetail` を指すようになりました。

**実機テスト（本番ビルド）**

```
クリックしたカード      : ビットコインは8月中に100,000ドルに到達するか？
クリック時のスクロール位置 : 3000
遷移後のパス           : /market/what-price-will-bitcoin-hit-in-august-2026
詳細ページのタイトル     : ビットコインは8月中に100,000ドルに到達するか？ ｜ 未来レーダー
判定                  : 詳細ページへ遷移 ✅
```

監査で指摘した「押した → 上に飛ばされた、の因果が画面上に見えない」状態は解消しました。
`App.tsx` に残る `window.scrollTo({top:0})` はページ遷移時のもので、**これは正しい挙動**です。

---

## ✅ C-4 — `iconUrl` が描画された

`AllMarketsGrid` と `SpreadRankingSection` のカードヘッダーに円形サムネイルが入りました。

**実機テスト**

```
img 総数   : 33
読込成功   : 33  ✅
読込失敗   : 0
非表示化   : 0
サイズ     : 16×16
alt        : ""（装飾扱い・隣にカテゴリラベルがあるため適切）
src        : https://polymarket-upload.s3.us-east-2.amazonaws.com/...
```

`onError` でフォールバック（非表示化）も実装されており、**壊れた画像が出ない設計**です。
監査時点で `<img>` 0件だったものが、33件すべて正常に表示されています。

> 軽微：`loading="lazy"` が未設定です。33枚が初期ロードで一斉に読み込まれます。
> 現状の枚数なら実害は小さいですが、銘柄数が増えると効いてきます。

---

## ✅ デッドコード削除

`EventCard.tsx` / `HeroFeatured.tsx` が**物理削除**されました。第8回の提案どおりです。

---

## ⚠️ D-6 — 「定義の重複」は解消、「表示の重複」は残存

### できたこと

```
src/types/index.ts:3   export const UNIFIED_CATEGORIES = [...]
Header.tsx:5           import { UNIFIED_CATEGORIES } from '../types'
AllMarketsGrid.tsx:2   import { UNIFIED_CATEGORIES } from '../types'

ハードコード定義の残存 : 0件 ✅
'newest' ソートの残存  : 0件 ✅（選択肢は volume / gap / trending）
```

定義が1箇所に統合され、未実装ソートも削除されました。**保守面の問題は解消しています。**

### 未対応：ナビゲーションは依然2セット

```
ヘッダーのタブ数     : 7
グリッドのピル数     : 7
```

監査の D-6 で挙げたのは次の点でした。

> 「カテゴリナビが同一ページに2セット。定義も2箇所にコピペされている」
> 「**利用者から見れば同じタブが2つあり、どちらを押せばよいか分かりません**」

**副次的な問題（定義のコピペ）は直り、主たる問題（画面上の重複）は残っています。**
同じ7項目のナビが1画面に2回出る状態は変わっていません。

どちらか一方に寄せるか、役割を分ける（ヘッダー＝大分類、グリッド＝絞り込み）などの
情報設計上の判断が必要です。

---

## 🆕 NEW-6 — A-3 の是正が `AllMarketsGrid` に適用されていない

第9回の検証中に発見しました。**A-3 と同じ「少数票で乖離を主張する」問題が、別コンポーネントに残っています。**

### 2つのコンポーネントで基準が違う

```js
// SpreadRankingSection.tsx:36 — A-3 で修正済み
const pool = events.filter(ev => ev.japanVotes.total >= 3);   // 3票以上に限定
// カードには n=3 / n=5 と票数を併記 ✅

// AllMarketsGrid.tsx:184 — 未修正
const hasGap = event.japanVotes.total > 0 && gap >= 10;       // 1票でも表示
// 票数の表示なし ❌
```

### 実データでの影響

```
投票のある銘柄       : 23
  1票  :  9銘柄
  2票  :  7銘柄   ← 計16銘柄（70%）が3票未満
  3票  :  4銘柄
  4票  :  2銘柄
  5票  :  1銘柄
```

画面上、**7枚のカードに「◯% 乖離」バッジが出ており、いずれも票数の表示がありません。**

```
ビットコインは8月中に100,000ドルに到達するか？   世界 1%   「49% 乖離」  日本(ロック中)
米FRB：9月FOMCで50bp以上の大幅利下げを実施するか？ 世界 1%   「49% 乖離」  日本(ロック中)
パリ・サンジェルマンは2026-27 UEFAチャンピオンズ… 世界 14%  「86% 乖離」  日本(ロック中)
```

3行目の「86% 乖離」は、ランキング側では **n=3** と併記されている同じ銘柄です。
**同じ数字が、同じ画面の別セクションでは票数つき、カードでは票数なしで出ています。**

さらに、日本世論の数値自体は「🔒 投票で解禁」で伏せられているのに、
乖離バッジからは日本側の値（50%や100%）が逆算できます。バイアス回避というゲートの設計意図とも矛盾します。

**対策**：`AllMarketsGrid` の `hasGap` を `japanVotes.total >= 3` に揃え、バッジに `n=◯` を併記してください。
`SpreadRankingSection` と同じ1行の変更です。

---

## ❌ 未達（4件）

| ID | 内容 |
|---|---|
| `A-7` | コメントが保存されない |
| `B-3` | `index.css` 単一・`!important` 過多 |
| `E-4` | クリック可能な非ボタン要素（`tabIndex` / `onKeyDown` 0件） |
| `F-3` | 手組みルーティング / Hooks違反 |

`D-4`（固定バーの誤爆）と `F-1`（CSS未分割）、`NEW-2`（dev/本番の CSS 差異）も継続です。

---

## ✅ 維持されている指標（回帰ゼロ・4ラウンド連続）

| 指標 | 第8回 | 今回 |
|---|---:|---:|
| タップ 44px 未満 | 0% | **0%**（0/150） |
| 12px 未満のテキスト | 3% | **3%**（最頻値 12.0px・10px未満 4件） |
| 英語・破綻タイトル | 0件 | **0件** |
| 画面上の過去日付 | 0件 | **0件** |
| トップページの `h1` | 1 | **1** |
| 画像 | 0件 | **33件**（C-4 で新規） |
| 型チェック | exit 0 | **exit 0** |
| JS 初期チャンク | 606.7kB | **606.9kB** |

---

## 次にやるべきこと

### 今すぐ（10分）

1. **NEW-6** — `AllMarketsGrid.tsx:184` の `hasGap` を `japanVotes.total >= 3` に変更し、`n=◯` を併記。
   `SpreadRankingSection` に合わせるだけです。

### 短期

2. **D-6 の本丸** — カテゴリナビをどちらか一方に寄せる。情報設計の判断が要る唯一の残件です。
3. **`E-4`** — `<tr onClick>` / `<div onClick>` のキーボード対応。
4. `loading="lazy"` をアイコン画像に追加。

### 中期

5. **`A-7`** コメントの永続化（または機能ごと非表示）
6. **`F-1` / `B-3`** 管理コンソール CSS の切り出しと `index.css` の分割
7. **`F-3`** ルーティングの整理

---

## 所感

**未達が7件から4件に減り、監査開始時の21件から5分の1以下になりました。**
D-5 と C-4 はどちらも実機で動作を確認でき、とくに C-4 は33枚すべてが正常表示・`onError` フォールバック付きで、
「とりあえず `<img>` を置いた」以上の実装になっています。

一方、今回も**同じパターンが1つ出ました。**
`SpreadRankingSection` で直した乖離の基準が、`AllMarketsGrid` に反映されていません（NEW-6）。
これは NEW-4 のガード漏れ、E-2 のセレクタ漏れと同じ構図です。

D-6 の対応も同型でした。「定義を1箇所に」という**実装上の重複**は直り、
「画面に2セット出ている」という**利用者から見た重複**は残っています。

3回続いているので、パターンとして挙げておきます。
**同じ処理・同じ表示をしている箇所を先に列挙してから直す**と、この取りこぼしは止まります。

```bash
# 例：乖離バッジを出している箇所を全部洗う
grep -rn "乖離" src/components/*.tsx
```

残る4件は、いずれも規模が大きめの構造リファクタリング（`B-3` `F-3`）か、
機能判断が要るもの（`A-7`）です。**ここまでで「壊れているから直す」フェーズは終わり、
「良くするために作り変える」フェーズに入っていると思います。**

---

## 付録：検証手順

```bash
npx vite build
npx vite preview --host 127.0.0.1 --port 4173
npx tsc -b
```

乖離バッジの基準を突き合わせる:

```bash
grep -n "japanVotes.total" src/components/*.tsx
# SpreadRankingSection: >= 3   AllMarketsGrid: > 0   ← 不一致
```

票数分布の確認:

```bash
curl -s "$VITE_SUPABASE_URL/rest/v1/japan_vote_logs?select=event_id,choice&limit=2000" \
  -H "apikey: $VITE_SUPABASE_ANON_KEY" -H "Authorization: Bearer $VITE_SUPABASE_ANON_KEY"
```
