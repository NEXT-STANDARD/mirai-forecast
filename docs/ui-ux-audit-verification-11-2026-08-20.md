# 再修正 検証レポート（第11回）

**対象**: mirai-forecast / 第10回検証の指摘に対する再修正
**検証日**: 2026-08-20
**検証方法**: ソース照合 ＋ 本番ビルド実機計測 ＋ **自己検証エンジンの破壊テスト（新手口を追加）**

---

## 総括

**指摘した2点はどちらも大きく改善しました。**

| 判定 | 第1回 | … | 第9回 | 第10回 | 今回 |
|---|---:|---:|---:|---:|---:|
| ✅ 完全に修正（35項目中） | 9 | … | 28 | 30 | **30** |
| ⚠️ 部分的・不完全 | 5 | … | 3 | 2 | **2** |
| ❌ 未達 | 21 | … | 4 | 3 | **3** |

- **NEW-7**（カテゴリの埋没）は解消。しかも D-6（重複）と両立させています
- **自己検証エンジン**は 7項目 → 8項目に増え、検知率が **1/3 → 4/5** に向上

ただし、検証の過程で次の2点が残っていることを確認しました。

1. **ヘッダーは `position: sticky` を指定しているのに、実際には固定されていません**（NEW-8）
2. **自己検証エンジンに穴が1つ残ります**（`0 &&` による無効化が素通り）

---

## ✅ NEW-7 — カテゴリが初期表示に戻り、重複もない

```
カテゴリタブの位置   : 画面上端から 55px
初期表示で見えるか   : true ✅
ヘッダーのタブ数     : 7
グリッドのピル数     : 0   ← 重複は発生していない ✅
切替動作            : スポーツ選択で 30枚 → 23枚 ✅
```

第9回の D-6（重複の解消）と第10回の NEW-7（到達性の確保）を**両立**させています。
「消す側」だけでなく「残す側の配置」まで含めて設計し直されました。

自己検証エンジンにも `カテゴリナビ配置・単一性` のチェックが追加されており、
指摘された内容がそのまま回帰テストになっています。**この動きは良いと思います。**

---

## ⚠️ NEW-8 — ヘッダーが sticky 指定なのに固定されない

報告には「**スクロール追従するヘッダー直下に**配置」とありますが、実測では追従しません。

```
スクロール位置 3000px の時点

ヘッダー           : position: sticky / top: 0px（指定はある）
ヘッダーの実位置    : -3000 〜 -2902px   ← 画面外へ流れている
カテゴリバーの位置   : -2950 〜 -2903px   ← 同上
```

15,000px 超のページで、**一度スクロールするとカテゴリ切り替えに戻れません。**
初期表示での到達性は確保されましたが、「追従」は成立していません。

### 原因（`src/index.css:7525-7531`）

```css
html, body, #root {
  max-width: 100vw !important;
  overflow-x: hidden !important;   /* ← これが sticky を無効化している */
  position: relative;
  width: 100%;
  box-sizing: border-box;
}
```

`overflow-x: hidden` を指定すると、仕様上 `overflow-y` が `auto` に昇格し、
`html` / `body` / `#root` がスクロールコンテナになります。
`position: sticky` はビューポートではなく**そのコンテナ**を基準にするため、固定されません。

実測でも3階層すべてが `overflow: hidden auto` になっていることを確認しました。

### 検証済みの修正案

`overflow-x` を `hidden` から `clip` に変えると解決します。ブラウザ上で実際に試しました。

```
変更前  ヘッダー : -3000px（画面外）
変更後  ヘッダー : 0 〜 98px        ← 固定された ✅
        カテゴリバー : 50 〜 97px    ← スクロール中も見える ✅
        横スクロール : 0px           ← hidden の目的も維持 ✅
```

`clip` はスクロールコンテナを作らないため、`sticky` を壊さずに横スクロールだけを抑止できます。
**`hidden` を `clip` に置き換える1語の変更**で足ります。

> なお、この CSS ブロックは監査開始時点から存在しており、**ヘッダーは一度も固定されていませんでした。**
> 今回カテゴリをヘッダーに戻したことで、初めて実害として表面化した形です。

---

## 🛡️ 自己検証エンジン — 検知率 1/3 → 4/5 に向上、穴は1つ残る

`check #2` に `strictGuardPattern`、`check #5` に `extractOpeningTags`（全ファイル走査）が入りました。
項目も 7 → 8 に増えています。

前回と同じ手口＋**報告に書いていない新しい壊し方**を注入して試しました。
（テスト後、両ファイルとも `diff` で完全一致を確認して復元済みです。）

| # | 注入した回帰 | 前回 | 今回 |
|---|---|---|---|
| A | 乖離基準を `>= 3` → `> 0` | ✅ 検知 | ✅ 検知 |
| B | `false && event.isExpired` で無効化 | ❌ 素通り | ✅ **検知** |
| C | `onKeyDown` を全削除 | ❌ 素通り | ✅ **検知** |
| D | **比較を反転**（`< Date.now()` → `> Date.now()`） | — | ✅ **検知** |
| E | **`tabIndex` だけ削除**（`onKeyDown` は残す） | — | ✅ **検知** |
| F | **`0 &&` で無効化**（`false` ではなく `0`） | — | ❌ **素通り（8/8合格）** |

D と E は私の報告に書いていない手口です。**それでも検知できたのは、
`strictGuardPattern` がガード条件の構造そのものを検査しているからで、本物の強化です。**

### 残る穴：`0 &&` が素通りする理由

```js
const strictGuardPattern = /(?:event|item)\.isExpired\s*\|\|\s*\(.../;   // 部分一致
const falsifiedPattern   = /false\s*&&\s*(?:event|item)\.isExpired/;      // ブラックリスト
```

照合結果を実際に確かめました。

```
正常                strict:一致  falsified:見逃し  → PASS（正しい）
false && で無効化    strict:一致  falsified:検知    → FAIL（正しい）
0 && で無効化        strict:一致  falsified:見逃し  → PASS（★素通り）
```

`strictGuardPattern` は**文字列のどこかに**canonical な条件式があれば通ります。
`{0 && event.isExpired || (...)` は canonical 部分をそのまま含むため一致してしまい、
接頭辞の `0 &&` は `falsifiedPattern`（`false &&` 限定のブラックリスト）に引っかかりません。

`null &&` `undefined &&` `!1 &&` なども同様に通ります。

### 修正案：ブラックリストではなくアンカーで

JSX の条件式の**先頭**であることを要求すれば、接頭辞を挟む余地がなくなります。

```js
// { の直後に isExpired が来ることを要求する
const strictGuardPattern = /\{\s*(?:event|item)\.isExpired\s*\|\|\s*\(\s*(?:event|item)\.endDate\s*&&\s*new Date\((?:event|item)\.endDate\)\.getTime\(\)\s*<\s*Date\.now\(\)\s*\)/;
// これなら falsifiedPattern（ブラックリスト）自体が不要になります
```

---

## ❌ 未達（3件・変化なし）

| ID | 内容 |
|---|---|
| `A-7` | コメントが保存されない |
| `B-3` | `index.css` 単一・`!important` 過多 |
| `F-3` | 手組みルーティング / Hooks違反 |

`D-4`（固定バーの誤爆）と `F-1`（CSS未分割）、`NEW-2`（dev/本番の CSS 差異）も継続です。

---

## ✅ 維持されている指標（回帰ゼロ・6ラウンド連続）

| 指標 | 第10回 | 今回 |
|---|---:|---:|
| タップ 44px 未満 | 0% | **0%** |
| 12px 未満のテキスト | 3% | **4%**（最頻値 12.0px・10px未満 4件） |
| 英語・破綻タイトル | 0件 | **0件** |
| 乖離バッジの n= 併記率 | 100% | **100%** |
| カテゴリUIの箇所 | 1（グリッド） | **1（ヘッダー）** |
| トップページの `h1` | 1 | **1** |
| 型チェック | exit 0 | **exit 0** |

---

## 次にやるべきこと

### 今すぐ（1語の変更）

1. **NEW-8** — `src/index.css:7527` の `overflow-x: hidden !important` を
   `overflow-x: clip !important` に変更。ブラウザ上で効果を確認済みです。

### 短期

2. **自己検証エンジン** — `strictGuardPattern` を `\{\s*` でアンカーし、`falsifiedPattern` を廃止。
   ブラックリストは書いた人が思いつく壊し方しか防げません。

### 中期（構造リファクタリング）

3. **`A-7`** コメントの永続化（または機能ごと非表示）
4. **`B-3` / `F-1`** `index.css` の分割と管理コンソール CSS の切り出し
5. **`F-3`** ルーティングの整理

---

## 所感

**NEW-7 の対応は、これまでで最も設計として筋が通っています。**
第9回で「どちらか一方に寄せる」、第10回で「残す側の配置も設計のうち」と指摘した点が、
両方まとめて解決されました。カテゴリは1箇所・初期表示内・切替も動作。
さらに、その条件を自己検証エンジンのチェック項目として固定しています。

**自己検証エンジンの強化も本物でした。**
私が報告に書いていない壊し方（比較の反転、`tabIndex` だけ削除）を検知できたことが、それを示しています。
文字列の有無を見るだけだった前回とは質が違います。

残る穴は、`falsifiedPattern` という**ブラックリスト**に依存している部分です。
`false &&` は塞がれましたが `0 &&` は通ります。これは対症療法の限界で、
`strictGuardPattern` を `{` にアンカーする**ホワイトリスト方式に寄せれば構造的に解決**します。

NEW-8 については、指摘というより発見の共有に近いものです。
`overflow-x: hidden` が sticky を壊すのは有名な落とし穴で、**監査開始時から埋まっていました。**
カテゴリをヘッダーに戻したことで初めて実害が出た、というだけです。
`clip` への1語変更で直ることは実機で確認しました。

---

## 付録：検証手順

```bash
npx vite build
npx vite preview --host 127.0.0.1 --port 4173
node scripts/audit_self_check.mjs
```

自己検証エンジンの破壊テスト（新手口を含む）:

```bash
cp src/components/SpreadRankingSection.tsx /tmp/bak

# F: 0 && で無効化（現状は素通りする）
sed -i '' 's/event.isExpired ||/0 \&\& event.isExpired ||/' src/components/SpreadRankingSection.tsx
node scripts/audit_self_check.mjs        # ← FAIL が出るべき

cp /tmp/bak src/components/SpreadRankingSection.tsx
diff /tmp/bak src/components/SpreadRankingSection.tsx   # 復元確認
```

sticky の検証:

```js
window.scrollTo(0, 3000);
document.querySelector('header').getBoundingClientRect().top;   // 0 付近であるべき
```
